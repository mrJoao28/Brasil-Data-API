services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: brazil-data-api:local
    container_name: brazil-data-api
    ports:
      - '${PORT:-3000}:3000'
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 3000
    restart: unless-stopped
